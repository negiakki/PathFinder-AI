/**
 * SkillChip — renders a single skill tag.
 * Full implementation: Phase 6.
 *
 * Props:
 *   label: string
 */
export default function SkillChip({ label }) {
  return (
    <span className="skill-chip">
      {label}
    </span>
  )
}
